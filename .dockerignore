# Keep the build context lean — Cloud Build uploads this whole thing.
# Match .gitignore + GSD planning + generated outputs.

# Node
**/node_modules
**/dist
**/.vite
**/coverage

# Env / secrets — never inside the image, runtime injects via Secret Manager
.env
.env.*
!.env.example

# Logs, scratch
**/*.log
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
.cache
.eslintcache

# OS / editor
.DS_Store
Thumbs.db
.idea
.vscode

# Repo-local tooling not needed for runtime.
# NOTE: scripts/ is intentionally NOT ignored — the Dockerfile runs
# scripts/gen-screenshots.mjs (and pwa-assets.config.ts) at build time to
# regenerate the gitignored PWA icons/screenshots into apps/web/public before
# vite bundles them. Without it the deployed manifest points at 404ing icons.
.planning
.gsd
.claude
deploy
docker-compose.yml
README.md
LICENSE
*.md
!apps/server/**/*.md
!apps/web/**/*.md

# Build cache / artifacts that should be regenerated inside the image
apps/web/dist
apps/web/dev-dist
apps/server/dist
packages/shared/dist

# Heavy generated PWA assets — these live in apps/web/public and need to ship,
# DON'T ignore that. Only ignore intermediate generator scratch.
.assets-cache

# Git
.git
.gitignore
.gitattributes

# Test load harness ships separately. Keep its package.json so npm workspaces
# stay consistent during install, but drop its source — nothing in the image
# needs it at runtime.
apps/bots/src
apps/bots/dist
apps/bots/tsconfig.json
apps/bots/node_modules
