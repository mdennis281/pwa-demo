{
  "name": "@pwa-demo/bots",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless load-test harness — spawns N socket.io clients that orbit + jump in a tower-climb lobby.",
  "bin": {
    "tower-bot": "./dist/index.js"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@pwa-demo/shared": "*",
    "dotenv": "^16.4.7",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
